# Desktop -> Marketplace integration (dev)
#
# The marketplace server runs on http://localhost:5174 in your setup.
# OAuth endpoints may or may not live under /oauth or /api/v1/oauth depending on the marketplace build.
# Set both so the desktop app hits the correct token endpoint.

VITE_MARKETPLACE_URL=http://localhost:5174
VITE_MARKETPLACE_OAUTH_URL=http://localhost:5174

# Must match the OAuth client redirect URI registered in the marketplace DB seed.
VITE_MARKETPLACE_REDIRECT_URI=http://localhost:51234/oauth/callback

