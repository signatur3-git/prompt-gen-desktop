# Desktop -> Marketplace integration (development only)
#
# Vite loads `.env.development` ONLY for dev mode.
# Put localhost overrides here so production builds default to Railway.

VITE_MARKETPLACE_URL=http://localhost:5174
VITE_MARKETPLACE_AUTH_URL=http://localhost:5174
VITE_MARKETPLACE_OAUTH_URL=http://localhost:5174/api/v1

# Must match the OAuth client redirect URI registered in the marketplace DB seed.
VITE_MARKETPLACE_REDIRECT_URI=http://localhost:51234/oauth/callback

